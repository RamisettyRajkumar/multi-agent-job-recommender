import React from 'react';

export default function App(){
  return (
    <div style={{padding:20}}>
      <h1>Job Recommender - Frontend (Optional)</h1>
      <p>This is a minimal React skeleton. Connect this to an API endpoint to call the Streamlit backend or a FastAPI service.</p>
    </div>
  );
}
